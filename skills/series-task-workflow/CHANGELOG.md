# series-task-workflow 版本台账

> 元数据规范：旧版快照在本目录 `versions/`，跨会话评测协议与数据在本目录 `eval/`。
> 全局安装副本以本目录的完整 skill 包为准；不得只同步 SKILL.md 与 references/ 而漏掉迁移、对账和测试脚本。

## v4 — 2026-08-30（主题＝与野蜂解除运行时双加载）

- `series-task-workflow` 明确成为未启用野蜂时的独立轻量系列协调器。
- 野蜂启用时不再额外加载本技能；检查点、Claim、登记表、评审、集成、清理、暂停/交接和成本熔断语义由野蜂内部自足实现。
- 保留本技能作为普通 batch/roadmap/audit set 的低成本入口，避免简单系列反向加载野蜂的角色、进程和消息治理。

## v3 — 2026-08-18（用户拍板即日生效；主题＝单执行者瘦身）

依据：本日 Claude Code 会话（RUL 波次 A 续跑：P-5 四审收口＋P-7）实测；用户当场裁定
「采纳瘦身、全局生效」。旧版快照：`versions/v2-2026-08-18/`（SKILL.md＋reconcile 脚本）。

| # | 变更 | 动机（v2 实测痛点） |
|---|---|---|
| 1 | `execution_mode: solo`：单执行者且无并发活跃 worker 时，claim 保留 id/scope/写入面/epoch，免 token/租约/时间戳簿记；resume 前对账实况取代租约过期作为陈旧守卫；派并发 worker 前须切回 multi 并恢复租约；solo 不放松写入面/WIP/门禁/评审任何规则（SKILL.md＋reconcile 脚本 soloMode 支持） | 波次 A 全程单执行者串行，租约/token/epoch 全套簿记防的竞争不存在；本日租约过期与登记"终审中"实为"从未启动"两处状态漂移都源于簿记与实况脱钩 |
| 2 | 账面批量化：每周期至多冻结、合并各 1 个记账提交，状态翻转不逐一提交；checkpoint 到 merged/closed 时登记行同步塌缩为状态＋合并号＋证据指针（归档即时化的执行细则） | P-5 分支 10 个提交 5 个是账面，集成线另压 5 个 `chore: record P5...`；登记行滚成 500 字全史（本就违反 v1 即有的归档条款，此条补执行细则） |
| 3 | 文档型检查点（验收＝事实准确性）可用单个轻量事实核对审替代完整评审合同 | 波次 A 把两路终审当默认规格用于一切件型，与 SKILL 自身"默认一审"脱节 |
| 4 | reconcile 脚本缺陷修复：git 调用加 `-c core.quotepath=false`、控制台输出编码强制 UTF-8——修中文路径下 `candidate_worktree_not_in_repository` 假警；新增 `execution_mode` 校验 | 本日实测：中文仓库路径被八进制转义/错码解码，工作区在册却报不在册，逼出人工证伪环节 |

验证状态：条 4 当日实测通过（同一状态对账由假警转全绿）；条 1-3 已在本日会话按新规程运行（P-5 单审收口、P-7 并行小件），后续长会话按 `eval/PROTOCOL.md` 继续对比。

## v2 — 2026-08-18（待验证）

依据：Codex 会话 `01a01353-29c3-7d42-9d3c-4cbe15aca055`（2026-08-17 波次 A，12h44m）复盘。
旧版快照：`versions/v1-2026-08-18/`。与 code-review-and-quality v2 配套发布。

| # | 变更 | 动机（v1 实测痛点） |
|---|---|---|
| 1 | 阻断发现分级 `product` / `administrative` / `infrastructure`：仅 product 计入候选/评审失败预算与熔断；行政类就地修正（补登记→重冻结→树哈希等值证明）不烧额度；基础设施类修复后重跑不计数 | R-4 第三轮 `review failed` 仅因登记表漏列 1 个文件，却触发完整熔断复位+全量门禁+重审，约 30 分钟零代码收益 |
| 2 | 门禁/评审证据绑定生产源码树哈希而非 commit SHA；doc/证据/登记-only 的替换候选继承先前结果并记录哈希等值证明 | R-4（dc179256→587b428）与 P-5 多次因只改证据文字全量重跑，每次约 5 分钟 |
| 3 | 门禁经济学：登记全门禁基线时长，执行窗口 ≥2× 基线或后台轮询；执行器截断记 infrastructure，第二次截断必须修窗口；同一冻结版本的分段运行等价于组合命令证据；迭代期聚焦门禁，全门禁只在冻结点与合并后 | ≥4 次门禁被 64/120/244/304 秒命令时限误杀；另有 1 次纯为"字面命令合规"重跑约 5 分钟 |
| 4 | 审查者任务书含证据信任边界+收敛预算：已登记门禁为可信输入，只做廉价定向探针，不得重跑全量门禁或扩验证矩阵；超预算无 verdict 即换人补同一槽位 | ≥4 次审查者自行膨胀成长任务（重跑门禁、自拆子审查、扩矩阵），全靠主线程临场中断 |
| 5 | 语义厚检查点实现前先冻结验收不变量清单+RED 矩阵并对其对抗审；后续评审对照冻结清单查漏，不逐轮重推导规格 | P-5 四轮评审每轮发现新的规格级问题（配平语义/题位定义/回流姿态/终态边界），收敛极慢 |
| 6 | control-state 效率计数器扩充：全门禁次数+树哈希继承次数、基础设施中断、行政修正、审查者更换，收尾时记录，供跨会话评测直接读取 | v1 评测只能从 27MB 会话日志反向解析 |

验证状态：**待验证**。按 `eval/PROTOCOL.md` 用下一个长系列会话对比 v1 基线（`eval/LEDGER.md` § v1 基线）。

## v1 —（基线，2026-08-18 快照）

内容：`versions/v1-2026-08-18/`（SKILL.md + references/control-state.md）。
运行样本：Codex 会话 01a01353（波次 A：P-8、P-1、R-4、P-6 完成，P-5 未收口），基线指标见 `eval/LEDGER.md`。
